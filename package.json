{
  "packageManager": "pnpm@9.12.2",
  "name": "carepanion-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo for the Carepanion project (Frontend, Backend, Smart Contracts)",
  "author": "GotGJee",
  "license": "UNLICENSED",
  "workspaces": [
    "packages/frontend",
    "packages/backend",
    "packages/smart-contract"
  ],
  "scripts": {
    "dev": "concurrently \"pnpm --filter packages/frontend dev\" \"pnpm --filter packages/backend dev\"",
    "build": "pnpm --filter packages/frontend build && pnpm --filter packages/backend build",
    "test": "pnpm --filter packages/smart-contract test"
  },
  "dependencies": {
    "psql": "^0.0.1"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
